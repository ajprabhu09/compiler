// use crate::{ast::Expr, token::Token};




// pub enum Assoc {
//     Left,
//     Right,
// }
// pub struct Priority(usize);

// pub struct InfixOp(Priority, Assoc);

// pub enum ExprOrOp<'a> {
//     Expr(Expr<'a>),
//     InfixOp(Token),
// }

// pub struct PrattParser<'a> {
//     pub stack: Vec<ExprOrOp<'a>>,
// }

// impl<'a> PrattParser<'a> {

//     pub fn push(&mut self, eop: ExprOrOp<'a>) {
        
//     }
// }
